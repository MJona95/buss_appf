name: buss_app
description: "A modern public and private transport app recreated from Zenith/BusGo designs."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.2.1
  sqflite: ^2.3.3
  path: ^1.9.0
  path_provider: ^2.1.3
  supabase_flutter: ^2.17.2
  url_launcher: ^6.3.0
  google_maps_flutter: ^2.17.1
  geolocator: ^14.0.2
  provider: ^6.1.5
  http: ^1.2.2
  shared_preferences: ^2.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
